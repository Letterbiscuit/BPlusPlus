#include <stdio.h>//For terminal and file IO
#include <stdint.h>//Because uint8_t makes purpose more obvious than char in a memory block
#include <unistd.h>//System calls to gcc
#include <stdlib.h>//For malloc and realloc



int main(int argc, char *argv[]){



	return 0;
}
